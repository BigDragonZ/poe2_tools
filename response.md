# 问答记录（仅保留最近一次）

> 规则：每次提问得到回答后，将回答内容写入本文档，覆盖旧内容，只保留最近一次。

## 2026-09-13 设计方案评审的确认结果

1. **poe1 代码复用方式**：直接复制 `poe_tools/` 模块到新项目再适配，不抽公共包。
2. **坐标体系**：POE2 与 POE1 一致，沿用两点标定 + CellSize 网格推导。
3. **地图 / 装备模块**：暂时只做占位，不开发。
4. **AHK**：不需要，本工具纯 Python。配置文件不再受 UTF-16 LE 限制。
5. **GUI 框架**：暂时使用 tkinter，后续功能丰富了改 Qt。
6. **changelog 规范**：命名 `changelog/2025-01-15-功能名.md`，超过 10 个合并进 `CHANGELOG.md` 后删除明细；规则写进 AGENTS.md 由 AI 自动维护。
7. **验证标准**：功能文档同步维护待完成清单，人工验证后更新。

### 新增工作流规则

- 每次提问的回答内容写到根目录 `response.md`，只保留最近一次。
