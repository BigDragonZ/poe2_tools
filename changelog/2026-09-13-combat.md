# 2026-09-13 战斗模块迁移

## 新增

- 迁移 `poe1_tools/poe_tools/combat.py` → `poe2_tools/combat.py`：战斗巡航（连点宏），支持鼠标左/右键、空格、Q/W/E/R，策略为禁用/连点/按住不放，POE2 失焦自动停止
- 战斗页接入功能：启停热键捕获设置、七键策略与间隔保存（写入 `[Combat]` 节）
- 主窗口：注册战斗启停热键（默认 F2）、状态区新增战斗状态、F12 与关窗时停止战斗宏并释放所有按键
- `tests/test_combat.py`：按键配置读写、非法值回退、宏启动前置检查等 7 项单元测试（共 25 项全部通过）

## 修复

- 测试夹具改为 autouse，避免单测读写真实 `poe2_tools.ini`
- 宏启动前置检查测试 mock 窗口检测，不依赖真实前台窗口
- `poe2_tools.ini`（本机标定配置）加入 .gitignore，不入库

## 人工验证通过

- 背包整理：F3/F4 标定、F1 落点准确、F12 急停、失焦自动停止（2026-09-13）
